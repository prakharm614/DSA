#include<stdio.h>
int main()
{
    int p,q,r;
    printf("Enter the number of rows :");
    scanf("%d",p);
    printf("Enter the number of column :");
    scanf("%d",q);

    for(int i=1;i<=p;i++)
    {
       // for(int r=1;r<=q;r++)
        printf("r");
    }
    printf("\n");
    return 0;
}